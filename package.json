{
  "name": "clicker-miniapp",
  "version": "1.0.0",
  "description": "",
  "main": "server/dist/src/function-entrypoint.js",
  "scripts": {
    "build:client": "cd client && yarn build",
    "build:server": "cd server && yarn build",
    "build:server:dev": "cd server && yarn build:dev",
    "build": "yarn build:server && yarn build:client",
    "build:dev": "yarn build:server:dev && yarn build:client",
    "start": "cd server && yarn start",
    "dev": "concurrently \"cd server && yarn dev\" \"cd client && yarn dev\" \"cd functions && yarn giveaway-scheduler\"",
    "dev:mobile": "concurrently \"cd server && yarn dev:mobile\" \"cd client && yarn dev:mobile\" \"cd functions && yarn giveaway-scheduler\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
